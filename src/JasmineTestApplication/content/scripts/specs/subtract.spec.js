describe('subtract tester', function () {

    it('add two numbers', function () {
        expect(subtract(1, 0)).toEqual(1);
        expect(subtract(2, 0)).toEqual(2);
        expect(subtract(3, 0)).toEqual(3);
        expect(subtract(4, 0)).toEqual(4);
    });

});